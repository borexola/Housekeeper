<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Pure logic only: no HTTP, no SQL, no hosting. Everything here is deterministic and unit-testable.
    The single dependency is the logging abstraction.
  -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.12" />
  </ItemGroup>

</Project>
